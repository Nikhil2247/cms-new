# =============================================================================
# CMS Frontend - Environment Template
# =============================================================================
# Copy this file to .env for local development
# For Docker deployment, build args are set in docker-compose.yml
# =============================================================================

# Backend API URL
# Development: http://127.0.0.1:5000/api (backend runs locally or via npm run dev)
# Docker: Set via docker-compose.yml build args
VITE_API_BASE_URL=http://127.0.0.1:5000/api

# Application Settings
VITE_APP_NAME=CMS Portal
VITE_APP_ENV=development
